export type Stock = {
	id: string;
	ticker: string;
	name: string;
};

export type StockResponse = Stock[];

export type Crypto = {
	id: string;
	ticker: string;
	name: string;
};

export type CryptoResponse = Crypto[];
